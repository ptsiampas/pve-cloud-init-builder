# Copy this file to users.env and replace the placeholders with your own values.
# Keys must stay quoted so multiline SSH keys render correctly.

PETERT_SSH_KEY_1='ssh-ed25519 AAAA... user@example'
PETERT_SSH_KEY_2='ssh-rsa AAAA... user@example'
PETERT_PASSWORD_HASH='$6$replace_me$hashed_password_value'
ANSIBLE_SSH_KEY_1='ssh-ed25519 AAAA... automation@example'
